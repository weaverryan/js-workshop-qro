diff --git a/city.js b/city.js
index 090bd44..303addc 100644
--- a/city.js
+++ b/city.js
@@ -12,14 +12,16 @@ module.exports = class {
 
     return new Promise((resolve, reject) => {
       setTimeout(() => {
+        reject();
+
         const stats = {
           name: this.name,
           population: getRandomNumber(100000),
         }
 
-        console.log(stats);
+        //console.log(stats);
 
-        resolve(stats);
+        //resolve(stats);
       }, 1000);
     });
   }
